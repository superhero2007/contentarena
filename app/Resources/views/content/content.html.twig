{% extends "layout.html.twig" %}

{% block stylesheets %}

    {{ parent() }}
    <link href="{{ asset('bundles/app/css/content.css') }}" type="text/css" rel="stylesheet" />

{% endblock %}

{% block main_content %}

	{{ include('content/contentInformation.html.twig', { 'content' : content }) }}

	{{ include('content/termSheet.html.twig', { 'content' : content }) }}

    {% if content.salesPackages | length > 0 %}

        {% for salespackages in content.salesPackages %}

            {{ include('content/salesPackage.html.twig', { 'package' : salespackages }) }}

        {% endfor %}

    {% endif %}

    {{ include('content/find_territories.html.twig', {'territories' : territories}) }}

{% endblock main_content %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('bundles/app/javascript/content.js') }}"></script>

{% endblock %}